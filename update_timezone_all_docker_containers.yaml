- name: Set and verify timezone for all running Docker containers on multiple hosts
  hosts:
    - server01.krabs.me
    - server04.krabs.me
  become: yes
  tasks:
    - name: Get list of running container IDs and names
      ansible.builtin.shell: |
        docker ps --format '{% raw %}{{.ID}}{% endraw %}\t{% raw %}{{.Names}}{% endraw %}'
      register: running_containers
      changed_when: false

    - name: Debug - Show raw output from docker ps
      ansible.builtin.debug:
        var: running_containers.stdout_lines

    - name: Set timezone in each running container
      when: running_containers.stdout_lines is defined and running_containers.stdout_lines | length > 0
      loop: "{{ running_containers.stdout_lines }}"
      ansible.builtin.shell:
        cmd: |
          LINE="{{ item }}"
          CONTAINER_ID=$(echo "$LINE" | cut -f1)
          CONTAINER_NAME=$(echo "$LINE" | cut -f2)
          TARGET_TIMEZONE="America/New_York"
          ZONEINFO_PATH="/usr/share/zoneinfo/$TARGET_TIMEZONE"

          if [ -z "$CONTAINER_ID" ] || [ -z "$CONTAINER_NAME" ]; then
            echo "Skipping malformed line: '$LINE' - Missing ID or Name."
            exit 0
          fi

          echo "Processing container: $CONTAINER_NAME ($CONTAINER_ID)"

          # Check for setup-timezone (Alpine, if available)
          if docker exec "$CONTAINER_ID" command -v setup-timezone >/dev/null 2>&1; then
            echo "  Using setup-timezone for $CONTAINER_NAME"
            docker exec -u 0 "$CONTAINER_ID" setup-timezone -z "$TARGET_TIMEZONE"

          # Check for /etc/timezone (Ubuntu/Debian)
          elif docker exec "$CONTAINER_ID" test -f /etc/timezone; then
            echo "  Manually setting timezone for $CONTAINER_NAME (Ubuntu/Debian style)"
            docker exec -u 0 "$CONTAINER_ID" sh -c "echo '$TARGET_TIMEZONE' > /etc/timezone && cp \"$ZONEINFO_PATH\" /etc/localtime"

          # Fallback for Alpine-like containers without setup-timezone, using symlink
          elif docker exec "$CONTAINER_ID" test -d /usr/share/zoneinfo/"$(dirname "$TARGET_TIMEZONE")"; then
            echo "  Setting timezone via /etc/localtime symlink for $CONTAINER_NAME (Alpine style fallback)"
            docker exec -u 0 "$CONTAINER_ID" sh -c "
              rm -f /etc/localtime && \
              ln -sf \"$ZONEINFO_PATH\" /etc/localtime
            "
          else
            echo "  Could not determine timezone setup method or zoneinfo files for $CONTAINER_NAME. Skipping."
          fi
      changed_when: false # Set to false to avoid reporting "changed" if the command itself just reports something, not makes a change

    - name: Report current timezone for each container
      when: running_containers.stdout_lines is defined and running_containers.stdout_lines | length > 0
      loop: "{{ running_containers.stdout_lines }}"
      ansible.builtin.shell:
        cmd: |
          LINE="{{ item }}"
          CONTAINER_ID=$(echo "$LINE" | cut -f1)
          CONTAINER_NAME=$(echo "$LINE" | cut -f2)

          if [ -z "$CONTAINER_ID" ] || [ -z "$CONTAINER_NAME" ]; then
            echo "Skipping malformed line for reporting: '$LINE'"
            exit 0
          fi

          # Use 'date' to get the current date and timezone
          # -u 0 (root) is used just in case the default user doesn't have permissions to run date in a way that shows timezone
          # or if the date command itself is in a root-only path (unlikely but safe)
          echo "Container '{{ CONTAINER_NAME }}' (ID: {{ CONTAINER_ID }}):"
          docker exec -u 0 "$CONTAINER_ID" date
      register: timezone_report
      changed_when: false # This task only reports, doesn't change anything

    - name: Display timezone report
      ansible.builtin.debug:
        msg: "{{ item.stdout_lines }}"
      loop: "{{ timezone_report.results }}"
      loop_control:
        label: "{{ item.item.split('\t')[1] | default(item.item) }}" # Show container name in label
      when: item.skipped is not defined and item.stdout_lines is defined